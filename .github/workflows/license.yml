name: license

on:
  pull_request:
  push:
    branches:
      - main
  merge_group:

permissions:
  contents: read

jobs:
  license-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - shell: bash
        run: |
          set -euo pipefail
          grep -q "Apache License" LICENSE
          grep -q "Apache-2.0" NOTICE
          if grep -qi "Elastic License" LICENSE; then
            exit 1
          fi
