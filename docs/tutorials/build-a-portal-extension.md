# Tutorial: Build A Portal Extension

1. Choose a shell slot such as `service.detail`.
2. Declare route, API ref, host ref, mount ref, and permissions.
3. Declare microfrontend runtime, sandbox, integrity ref, allowed events, and
   required context.
4. Keep service-specific UI outside platform core.
5. Run conformance and confirm portal checks pass.
